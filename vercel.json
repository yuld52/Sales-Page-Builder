{
  "buildCommand": "pnpm --filter @workspace/landing-page run build:vercel && mkdir -p dist && cp -r artifacts/landing-page/dist/. dist/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
