{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/landing-page run build:vercel",
  "outputDirectory": "artifacts/landing-page/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
