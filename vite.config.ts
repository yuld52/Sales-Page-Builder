// This file exists so Vercel detects this workspace as a Vite project (static site).
// The actual build is handled by the custom buildCommand in vercel.json.
import { defineConfig } from "vite";
export default defineConfig({});
